<h1>Editing User</h1>

<%= render 'form', user: @user %>

<% if current_user.is_admin? %>
  <%= link_to 'Back', admin_users_path %>
<% else %>
  <%= link_to 'Show', @user %> |
  <%= link_to 'Back', users_path %>
<% end %>
